<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:GreedySnake.Controls">


    <Style x:Key="SnakeBlockStyle" TargetType="{x:Type local:SnakeBlockControl}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:SnakeBlockControl}">
                    <ContentPresenter />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Grid.Row" Value="{Binding Position.Y}" />
    </Style>
</ResourceDictionary>
